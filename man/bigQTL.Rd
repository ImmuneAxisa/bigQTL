% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bigQTL.R
\name{bigQTL}
\alias{bigQTL}
\title{Run bigQTL: standard preparation and conditional QTL analysis}
\usage{
bigQTL(
  bigpheno,
  bigsnp,
  pheno_coord,
  design_base,
  n_pheno_pcs = 5,
  n_geno_pcs = 5,
  exclude_pheno_names = NULL,
  ...
)
}
\arguments{
\item{bigpheno}{bigPheno object with phenotype data (samples x phenotypes)}

\item{bigsnp}{bigSNP object with genotype data (samples x snps)}

\item{pheno_coord}{Data frame with pheno_name, chromosome, start, end columns}

\item{design_base}{Data frame with covariates; row names = sample IDs}

\item{n_pheno_pcs}{Number of phenotype PCs to compute and add as covariates
(default 5)}

\item{n_geno_pcs}{Number of genotype PCs to compute and add as covariates
(default 5)}

\item{exclude_pheno_names}{Character vector of phenotype names to exclude
from phenotype PCA (e.g. sex-chromosome phenotypes). Default NULL.}

\item{...}{Additional arguments passed to \code{run_conditional_qtl()}}
}
\value{
List with elements: stepwise (Arrow dataset), allbutone (Arrow dataset or NULL),
stepwise_dir, allbutone_dir
}
\description{
A convenience wrapper around \code{run_conditional_qtl()} that first
computes phenotype PCs and genotype PCs using the package helper
functions, appends them to \code{design_base}, and then runs the full
conditional QTL analysis.
}
