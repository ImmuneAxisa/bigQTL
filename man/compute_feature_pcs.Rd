% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_helpers.R
\name{compute_feature_pcs}
\alias{compute_feature_pcs}
\title{Compute feature principal components on file-backed matrix}
\usage{
compute_feature_pcs(
  bigfeatures,
  keep_ids = NULL,
  n_pcs = 5,
  n_top_features = 5000,
  exclude_col_idx = NULL,
  ncores = 1
)
}
\arguments{
\item{bigfeatures}{bigFeatures object with \code{$features} (FBM,
samples x features) and \code{$rowData} (data frame with column
\code{sample_name})}

\item{keep_ids}{Character vector of sample IDs to include. Must match
values in \code{bigfeatures$rowData$sample_name}. If NULL (default),
all samples are used.}

\item{n_pcs}{Number of principal components to retain (default 5)}

\item{n_top_features}{Number of top-variance features to use for PCA
(default 5000)}

\item{exclude_col_idx}{Integer vector of column indices to exclude from
PCA (e.g. sex-chromosome features). Default NULL.}

\item{ncores}{Number of cores for parallel computation (default 1)}
}
\value{
Matrix of feature PCs (n_kept_samples x n_pcs), with columns named
featurePC1, featurePC2, ..., featurePCn. Row names are the sample IDs
of the kept samples.
}
\description{
Runs PCA on feature (e.g. expression) data using bigstatsr, operating
directly on the FBM without loading data into memory. Uses
\code{bigstatsr::big_colstats()} to compute per-column variance on disk,
selects the top \code{n_top_features} by variance, then runs
\code{bigstatsr::big_randomSVD()} for PCA.
}
