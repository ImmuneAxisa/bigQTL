% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_helpers.R
\name{compute_pheno_pcs}
\alias{compute_pheno_pcs}
\title{Compute phenotype principal components on file-backed matrix}
\usage{
compute_pheno_pcs(
  bigpheno,
  keep_ids = NULL,
  n_pcs = 5,
  n_top_phenos = 5000,
  exclude_pheno_names = NULL,
  ncores = 1
)
}
\arguments{
\item{bigpheno}{bigPheno object with \code{$pheno} (FBM,
samples x phenotypes) and \code{$rowData} (data frame with column
\code{sample_name})}

\item{keep_ids}{Character vector of sample IDs to include. Must match
values in \code{bigpheno$rowData$sample_name}. If NULL (default),
all samples are used.}

\item{n_pcs}{Number of principal components to retain (default 5)}

\item{n_top_phenos}{Number of top-variance phenotypes to use for PCA
(default 5000)}

\item{exclude_pheno_names}{Character vector of phenotype names to exclude
from PCA (e.g. sex-chromosome phenotypes). Mapped to column indices via
\code{get_pheno_indices()}. Default NULL.}

\item{ncores}{Number of cores for parallel computation (default 1)}
}
\value{
Matrix of phenotype PCs (n_kept_samples x n_pcs), with columns named
phenoPC1, phenoPC2, ..., phenoPCn. Row names are the sample IDs
of the kept samples.
}
\description{
Runs PCA on phenotype (e.g. expression) data using bigstatsr, operating
directly on the FBM without loading data into memory. Uses
\code{bigstatsr::big_colstats()} to compute per-column variance on disk,
selects the top \code{n_top_phenos} by variance, then runs
\code{bigstatsr::big_randomSVD()} for PCA.
}
