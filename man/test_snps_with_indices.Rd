% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bigQTL.R
\name{test_snps_with_indices}
\alias{test_snps_with_indices}
\title{Test SNPs with given column indices using univariate linear regression}
\usage{
test_snps_with_indices(
  bigsnp,
  y,
  snp_indices,
  snp_names,
  design_base,
  ind.row,
  snp_conditioning = NULL,
  ncores = 1
)
}
\arguments{
\item{bigsnp}{bigSNP object with \code{$genotypes} (FBM) and \code{$map}}

\item{y}{Numeric phenotype vector (length = length(ind.row))}

\item{snp_indices}{Integer vector of column indices into the genotype FBM}

\item{snp_names}{Character vector of SNP names (same length as snp_indices)}

\item{design_base}{Data frame of covariates (row names = sample IDs)}

\item{ind.row}{Integer vector of row indices for samples in the genotype FBM}

\item{snp_conditioning}{Character vector of SNP names to condition on (default NULL)}

\item{ncores}{Number of cores for parallel computation (default 1)}
}
\value{
Data frame with columns: snp, beta, se, t_stat, pvalue, fdr
}
\description{
Test SNPs with given column indices using univariate linear regression
}
